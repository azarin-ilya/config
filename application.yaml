spring:
  main:
    banner-mode: off
  output:
    ansi:
      enabled: ALWAYS

management:
  tracing:
    sampling:
      probability: 1.0
  zipkin:
    base-url: http://localhost:9411

eureka:
  client:
    service-url:
      defaultZone: http://localhost:2020/eureka/
